<?xml version="1.0" encoding="UTF-8" ?>
<!-- Force FastDDS to use UDPv4 only (no shared memory) so a --network host
     container's participant is discovered by the host CarMaker bridge.
     SHM transport between container and host is the usual cause of the
     asymmetric-discovery / Subscription-count-0 failure. -->
<dds>
  <profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>udp_transport</transport_id>
        <type>UDPv4</type>
      </transport_descriptor>
    </transport_descriptors>
    <participant profile_name="udp_only" is_default_profile="true">
      <rtps>
        <userTransports>
          <transport_id>udp_transport</transport_id>
        </userTransports>
        <useBuiltinTransports>false</useBuiltinTransports>
      </rtps>
    </participant>
  </profiles>
</dds>
