# B1 — 튜닝된 nominal 베이스라인 실험 (2026-08-31)

## 왜 했나

기존 비교(슬라이드 17/22/23, `results/traj_*`)는 nominal의 비용 가중치를
**기본값 Q_n=1e-4** 로 두고 수행되었다. 그런데 이 기본값은 횡오차보다
**속도 추종(Q_v=1e-3)을 10배 더 중요하게** 여기는 설정이다. 논문의 평가 지표가
횡오차인데 nominal은 그것을 별로 최적화하지 않는 상태였던 셈이다.

"nominal을 제대로 튜닝하면?"에 답하기 위한 실험. 심사에서 반드시 나올 질문이다.

## 결과 요약

| 갈래 | 내용 | 핵심 |
|---|---|---|
| A 속도 스윕 | Q_n=1e-4 고정, 10/11.5/12 m/s | residual 이 세 속도 모두에서 이김 (58/35/17%) |
| B 가중치 스윕 | v=10, Q_n 1e-4→1e-2 (nominal 단독) | **가중치만으로 72% 개선** (0.847→0.240), 속도·랩타임 손실 없음 |
| C 적재 검증 | B의 최적 gain 하나로 32/48/56t | 단일 gain 이 전 적재에서 작동 (0.215/0.240/0.253) |
| D 상호작용 | 높은 gain 에서 잔차 세기 변경 | 이득 사라지고 채터만 증가, scale 0.3 에서는 발산 |

**정직한 결론:** residual 의 이득은 nominal 의 횡오차 가중치에 크게 의존한다.
Q_n=1e-4 에서 57.9% 였던 개선이 Q_n=1e-2 에서는 0.6% 로 줄어든다. 즉 보고된
이득의 상당 부분은 모델 보정이 아니라 **제어 게인 부족을 보상한 효과**다.

**다만** 동일 가중치 조건에서는 residual 이 진 적이 없으며, 11.5 m/s 에서는
추종 정확도와 **조향 부드러움을 동시에** 개선한다(채터 0.069→0.057).
12 m/s 에서 이득이 감소하는 것은 주행의 82% 가 학습 속도 상위 1%(12.03 m/s)를
벗어나는 외삽 구간이기 때문이다.

## 파일

모든 주행: Hockenheim 1랩, 정지 출발, 48t(별도 표기 없으면), 첫 랩만 평가.

| 파일 | 조건 |
|---|---|
| `v10_nom/res.npy` | Q_n=1e-4, v=10 (= `results/traj_{nom,ff}_48.npy` 사본) |
| `v115_nom/res.npy` | Q_n=1e-4, v=11.5 |
| `v12_nom/res.npy` | Q_n=1e-4, v=12 |
| `qn_{1e-4…1e-2}.npy` | v=10, nominal, 가중치 스윕 5점 |
| `qn_1e-2_{32t,56t}.npy` | Q_n=1e-2, v=10, 적재 검증 |
| `qn1e-2_v10_res_s003.npy` | Q_n=1e-2, v=10, 잔차 scale 0.03 |
| `qn1e-2_v12_{nom,res_s003,res_s01,res_s03}.npy` | Q_n=1e-2, v=12, 잔차 세기별 |
| `qn1e-2_v12_dagger_s01.npy` | 위와 같으나 DAgger 재학습 모델 |
|  `dagger_v12_tuned.npz` / `model_dagger_v12.pt` | 튜닝된 제어기로 수집한 데이터셋 (2,401 샘플) |

궤적 6열 = `[s, n, alpha, v, D, delta]`, dt=0.1 s.

## 재현

```bash
python3 results/b1_summary.py          # 표 4개 + results/matlab/fig_b1_summary.mat
python3 results/eval_b1.py results/b1/*.npy   # 개별 주행 지표
```

MATLAB: `results/matlab/plot_b1_summary.m`

## 주의 — DAgger 실험의 한계

 `dagger_v12_tuned.npz` / `model_dagger_v12.pt` 는 튜닝된 제어기로 수집해서 |n| 범위가 ±0.2 m 로 좁고
샘플이 2,401개뿐이다(기존 8,088). 이것만으로 학습한 모델은 성능이 오히려
나빴다(0.481). DAgger 는 원래 기존 데이터에 **누적**하는 방법이므로,
재시도한다면 기존 데이터와 합쳐서 학습해야 한다.

---

## 추가 (2026-09-02) — v = 14, 학습 분포 밖 외삽

속도 스윕에 **14 m/s** 를 추가했다. 기존 10/11.5/12 는 전부 학습 데이터
(`../ablation/hockenheim_nomass.npz`, 속도 최대 12.234 m/s) 안쪽이라 **일반화
주장이 전부 내삽**이었기 때문이다. 미학습 트랙(Highway No.1)도 마찬가지였다 —
핵심 4차원(n, α, δ, κ)이 학습 범위 내부이고 곡률은 오히려 완만했다
(최소반경 40 m vs 107 m).

| 파일 | 조건 |
|---|---|
| `v14_nom.npy` / `v14_res.npy` | Q_n=1e-4, 목표 14 m/s, 48 t, 건조 Hockenheim, 재학습 없음 |

잔차 모델은 `mpc/residual_model_nomass_s0.pt` (6차원 상태만, `hockenheim_nomass.npz`
학습). `RESIDUAL_FF=1`, `RESIDUAL_SCALE=0.3`, `DDELTA_MAX` 미설정(기존 스윕과 동일).

### 결과

| 목표 v | 완주 | nominal | residual | 개선 | 채터 nom | 채터 res |
|---|---|---|---|---|---|---|
| 10.0 | O/O | 0.854 | 0.359 | 57.9% | 0.0177 | 0.0645 |
| 11.5 | O/O | 1.067 | 0.695 | 34.8% | 0.0685 | 0.0566 |
| 12.0 | O/O | 1.099 | 0.908 | 17.4% | 0.0877 | 0.0589 |
| **14.0** | **O/O** | **2.138** | **1.211** | **43.4%** | 0.0620 | **0.0382** |

최고속도 14.38 m/s = 학습 상한의 **+17.5%**, residual 주행 샘플의 **34%**
(nominal 은 75%)가 학습 상한 밖이다.

### ⚠️ 제어 주기 정정 (2026-09-02) — 기존 잔차 주행은 6.5 Hz 였다

궤적 파일에 시간축이 없어 오랫동안 `DT = 0.1 s`(10 Hz)를 가정했다. 그러나
`ds/v` 로 역산하면 **v≤12 의 잔차 주행은 전부 0.155 s(6.5 Hz)** 다. solve 가
109 ms 라 10 Hz 주기를 못 지킨 결과다(`../solvetime/`). nominal 은 전부 10 Hz.

영향:

| | 기존(DT=0.1) | 정정 | |
|---|---|---|---|
| v10 res 채터 | 0.0645 | **0.0417** | −35% |
| v12 res 채터 | 0.0589 | **0.0380** | −35% |

**채터가 최대 35% 과대평가돼 있었다.** 횡오차·헤딩오차는 시간 미분이 없어
영향받지 않는다.

동시에 이는 **비교의 교란 요인**이다 — 잔차는 제어율이 35% 낮은 상태로
nominal 과 겨뤘다. 다만 방향이 잔차에 **불리**하므로 기존 결론(잔차 우세)은
보수적인 값이다. **v=14 는 SQP 수정 이후에 돌아 양쪽 다 10 Hz 이므로 유일하게
제어율이 맞는 비교다.**

### x / y / yaw 세 지표

횡오차 n 만 보고하면 "유리한 지표만 골랐다"는 반박을 받는다. 급코너 구간
RMS 로 α 를 같이 재면 두 지표가 **항상 같이 좋아지지는 않는다.**

`x` = 종방향 진행오차(목표속도 대비 부족분, 음수 = 뒤처짐),
`y` = 급코너 횡오차 RMS, `yaw` = 급코너 헤딩오차 RMS.

| 목표 v | 제어율 n/r | x_nom | x_res | y 개선 | yaw 개선 | 채터 개선 |
|---|---|---|---|---|---|---|
| 10.0 | 10.0 / 6.5 Hz | −31 m | −38 m | +57.9% | **−7.1%** | **−136%** |
| 11.5 | 10.0 / 6.5 Hz | −30 m | +19 m | +34.8% | +0.7% | +47% |
| 12.0 | 10.0 / 6.5 Hz | −30 m | +15 m | +17.4% | **−13.4%** | +57% |
| **14.0** | **10.0 / 10.0** | −171 m | **−573 m** | **+43.4%** | **+48.9%** | **+38.4%** |

**대가:** v=14 에서 잔차는 573 m 뒤처지고 랩타임이 29 s 길다(203 → 231 s).
정확도를 얻는 대신 목표 속도를 덜 지킨다. 이 비용을 함께 보고한다.

v=10 에서 α 가 나빠지는 것은 **횡오차를 줄이려고 차체를 더 적극적으로 돌리는
trade-off** 다(평균 0.57 → 0.81°). 절대값은 3° 이하로 작다. 같은 조건에서
조향 채터는 4배 가까이 늘어난다(0.0177 → 0.0645) — 이 채터는
`DDELTA_MAX=0.262`(15 °/s)를 걸면 48% 줄어든다(`../ddelta/`).

**외삽 조건 v=14 에서는 세 지표가 모두 개선된다.** 발표에서는 v=10 의 α·채터
악화를 먼저 밝히고 v=14 를 주력으로 쓰는 편이 방어된다.

### 왜 14 인가 (16/18 이 아니라)

`constraint.alat_max = 5.0 m/s²` 가 최급코너(κ=0.0265, R=38 m) 속도를
**13.7 m/s 로 묶는다.** 목표를 16 이나 18 로 올려도 코너 속도는 같고 직선만
빨라진다 — 잔차가 일하는 곳은 코너이므로 외삽 정도가 늘지 않는다. 게다가
`along_min = -10 m/s²` 는 48 t 트럭이 낼 수 없는 감속이라, 목표 18 에서는
못 지킬 감속을 계획해 코너에 과속 진입한다. 그러면 둘 다 실패하되 원인이
"외삽 실패"인지 "제동 능력 부족"인지 구분되지 않는다.

### 외삽 지표에 대한 주의

**거리 기반 지표를 쓰지 않았다.** 정규화 6차원 최근접거리로 재면 v=10 주행이
96%, v=14 가 80~92% 로 순서가 뒤집힌다 — 학습 데이터가 11.9 m/s 부근에 몰려
있어 v=10 도 "멀지만" 그건 **더 느려서** 먼 것이라 외삽이 아니다. 방향을
구분하는 지표(학습 속도 최대값 초과 비율)만 주장에 쓴다.

### 한계

- 각 조건 1회 주행 — 주행 간 편차 미측정
- v=14 에서 residual 실제 평균속도가 11.05 로 nominal 12.67 보다 낮다.
  "느려서 정확한 것 아니냐"는 반박이 가능하나, v=10 에서는 residual 이
  **더 빠르면서**(10.08 vs 9.90) 더 정확했으므로 속도-정확도 트레이드오프로는
  설명되지 않는다
- 튜닝된 nominal(Q_n=1e-2)과의 v=14 비교는 미실시

분석: `python3 results/extrapolation_summary.py` → `results/matlab/fig_extrapolation.mat`
MATLAB: `results/matlab/plot_extrapolation.m`
